# Axialert — Python dependencies (ThingsBoard-first, no local DB)
# Install with: pip install -r requirements.txt

# Web framework (ingestion server)
flask>=3.0.0

# HTTP client (ThingsBoard push + ESP32 feedback)
requests>=2.31.0

# ML inference (loaded once at startup, scores inline per request)
scikit-learn>=1.4.0
numpy>=1.26.0
joblib>=1.3.0
torch>=2.2.0

# Training pipeline only (run once to produce models/)
pandas>=2.2.0
tqdm>=4.66.0
